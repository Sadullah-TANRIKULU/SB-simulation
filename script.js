class Product {
  constructor(name, cost, expense) {
    this.stock = stock;
    this.name = name;
    this.cost = cost;
    this.expense = expense;
  }
}

class Customer {
  constructor(name, want) {
    this.name = name;
    this.want = want;
  }
}


// disable invest product button if not invested

// deliver product buttons
